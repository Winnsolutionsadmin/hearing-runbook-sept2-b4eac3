# hearing runbook (published mirror)
## UPSTREAM SOURCE OF TRUTH
This repo is a DOWNSTREAM, public, unlisted mirror of a single HTML artifact. The canonical upstream auto-loads via the operator's local ~/.claude registry; it is deliberately not committed here because this repo is public.
